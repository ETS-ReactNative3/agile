import { StyleSheet } from 'react-native';
import metrics from '../../../../config/metrics';

const styles = StyleSheet.create({
    image: { height: metrics.screenHeight * .85, width: metrics.screenWidth },
});

export default styles;
